import 'package:flutter/material.dart';

class ReportesPageTra extends StatelessWidget {
  const ReportesPageTra({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Reportes'),
      ),
      body: const Center(
        child: Text('Reportes'),
      ),
    );
  }
}